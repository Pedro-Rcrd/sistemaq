@model IEnumerable<systemquchooch.Models.Carrera>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<p>
    <a asp-controller="Carreras" asp-action="ImprimirCarrera" target="_blank" class="btn btn-info text-white">Generar PDF</a>
</p>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CodigoCarrera">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CodigoCarrera">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CodigoCarrera">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
